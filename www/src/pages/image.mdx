import {Image} from 'sassyboi'
import PropsTable from '../components/PropsTable'

export const meta = {
  name: 'Image',
  component: Image,
  description: 'An image with optional lazy loading and fade-in animation.',
  inheritBoxProps: true,
  playroom: '',
}
