import {ContentBlock} from 'sassyboi'
import PropsTable from '../components/PropsTable'

export const meta = {
  name: 'ContentBlock',
  component: ContentBlock,
  description: 'A container with max-width and padding built-in.',
  inheritBoxProps: true,
  playroom: '',
}
